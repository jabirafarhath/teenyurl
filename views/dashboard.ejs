<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Dashboard</title>
</head>
<body style="background-color:#a9b5c7;">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
              <a class="navbar-brand" href="#">teenyurl</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                </ul>
                <form class="d-flex">
                    <a class="nav-link disabled"><%= name %></a>
                    <a href="/logout" class="btn btn-outline-light">Logout</a>
                </form>
              </div>
            </div>
          </nav>
        <form action="/newUrls" method="post" class="mt-3">
            <label for="originalUrl" class="visually-hidden">Original Url</label>
            <input type="url" required name="originalUrl" id="originalUrl" placeholder="Url" required class="form-control">
            <button type="submit" class="btn btn-dark mt-3">Shrink</button>
        </form>
        <h6 class="mt-3">Your teenyurl is:</h6>
        <% if(newUrls==null){ %>
            <p><a href="" class=""></a></p>
        <%}else{ %>
            <p><a href="<%= newUrls[newUrls.length-1].new %>" class="" target="_blank"><%= newUrls[newUrls.length-1].new %></a></p>
        <% } %> 
    </div>
</body>


</html>
